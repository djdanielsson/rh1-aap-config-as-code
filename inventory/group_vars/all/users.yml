---
# Common Users structure across all environments
# Using _all suffix for wildcard merging

aap_user_accounts_all:
  # Security Team Members
  - username: sec_alice
    password: "{{ vault_user_password_sec_alice | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: alice@security.local
    first_name: Alice
    last_name: Security
    is_superuser: false
    is_system_auditor: true
    update_secrets: false

  - username: sec_bob
    password: "{{ vault_user_password_sec_bob | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: bob@security.local
    first_name: Bob
    last_name: Security
    is_superuser: false
    is_system_auditor: true
    update_secrets: false

  - username: sec_charlie
    password: "{{ vault_user_password_sec_charlie | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: charlie@security.local
    first_name: Charlie
    last_name: Security
    is_superuser: false
    is_system_auditor: true
    update_secrets: false

  # Platform Engineers Team Members
  - username: plat_david
    password: "{{ vault_user_password_plat_david | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: david@platform.local
    first_name: David
    last_name: Engineer
    is_superuser: true
    is_system_auditor: false
    update_secrets: false

  - username: plat_emma
    password: "{{ vault_user_password_plat_emma | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: emma@platform.local
    first_name: Emma
    last_name: Engineer
    is_superuser: true
    is_system_auditor: false
    update_secrets: false

  - username: plat_frank
    password: "{{ vault_user_password_plat_frank | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: frank@platform.local
    first_name: Frank
    last_name: Engineer
    is_superuser: true
    is_system_auditor: false
    update_secrets: false

  # Operations Team Members
  - username: ops_grace
    password: "{{ vault_user_password_ops_grace | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: grace@operations.local
    first_name: Grace
    last_name: Operations
    is_superuser: false
    is_system_auditor: false
    update_secrets: false

  - username: ops_henry
    password: "{{ vault_user_password_ops_henry | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: henry@operations.local
    first_name: Henry
    last_name: Operations
    is_superuser: false
    is_system_auditor: false
    update_secrets: false

  - username: ops_iris
    password: "{{ vault_user_password_ops_iris | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: iris@operations.local
    first_name: Iris
    last_name: Operations
    is_superuser: false
    is_system_auditor: false
    update_secrets: false

  # Application Developers Team Members
  - username: app_jack
    password: "{{ vault_user_password_app_jack | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: jack@applications.local
    first_name: Jack
    last_name: Developer
    is_superuser: false
    is_system_auditor: false
    update_secrets: false

  - username: app_kate
    password: "{{ vault_user_password_app_kate | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: kate@applications.local
    first_name: Kate
    last_name: Developer
    is_superuser: false
    is_system_auditor: false
    update_secrets: false

  - username: app_leo
    password: "{{ vault_user_password_app_leo | default(lookup('ansible.builtin.password', '/dev/null', chars=['ascii', 'digits', '.,:-_'], length=16)) }}"
    email: leo@applications.local
    first_name: Leo
    last_name: Developer
    is_superuser: false
    is_system_auditor: false
    update_secrets: false
...

