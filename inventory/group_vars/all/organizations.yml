---
# Common Organizations across all AAP environments
# These will be applied to dev, qa, and prod
# Using _all suffix for wildcard merging with dispatch role

controller_organizations_all:
  - name: Default
    description: Default organization
    max_hosts: 0
    galaxy_credentials:
      - Ansible Galaxy

  - name: Platform
    description: Platform and infrastructure automation
    max_hosts: 0
    galaxy_credentials:
      - Ansible Galaxy

  - name: Applications
    description: Application deployment and configuration
    max_hosts: 0
    galaxy_credentials:
      - Ansible Galaxy
...
