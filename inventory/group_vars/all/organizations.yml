---
# organizations.yml
# Common Organizations across all AAP environments
# These will be applied to dev, qa, and prod
#
# Naming format: lowercase, underscores only
# Variable suffix: _all (shared across all environments)

aap_organizations_all:
  - name: platform
    description: Platform and infrastructure automation
    max_hosts: 0
    # galaxy_credentials:
    #   - Ansible Galaxy

...
