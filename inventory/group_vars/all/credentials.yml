---
# credentials.yml
# Shared credentials used across multiple job templates
# Define each credential once here; JT files reference them by name
#
# Naming format: {org}_{type}_{identity}_{target}
# Variable suffix: _all (shared across all JTs)

controller_credentials_all:
  # Machine credentials (SSH access)
  - name: "plat_machine_svc_ansible_linux"
    description: "SSH access (svc_ansible user) to platform Linux servers"
    credential_type: "Machine"
    organization: "platform"
    # inputs:
    #   username: svc_ansible
    #   ssh_key_data: "{{ svc_ansible_ssh_priv_key }}"
    #   ssh_key_unlock: "{{ svc_ansible_ssh_key_unlock }}"
    #   password: "{{ svc_ansible_machine_pass }}"

  # Source control credentials
  - name: "plat_scm_djdanielsson_github"
    description: "GitHub access (git user) for platform repositories"
    credential_type: "Source Control"
    organization: "platform"
    # inputs:
    #   username: djdanielsson
    #   ssh_key_data: "{{ ssh_priv_key }}"
    #   ssh_key_unlock: "{{ ssh_key_unlock }}"

  # Container registry credentials
  - name: "plat_registry_svc_builder_quay_io"
    description: "Container registry access (svc_builder) for platform EE pulls"
    credential_type: "Container Registry"
    organization: "platform"
    # inputs:
    #   host: "{{ aap_hostname }}"
    #   username: "{{ aap_username }}"
    #   password: "{{ aap_admin_password }}"

  - name: "plat_aap_admin_aap"
    credential_type: Red Hat Ansible Automation Platform
    organization: "platform"
    description: "AAP admin account"
    # inputs:
    #   host: "{{ aap_hostname }}"
    #   username: "admin"
    #   password: "{{ aap_admin_password }}"
...